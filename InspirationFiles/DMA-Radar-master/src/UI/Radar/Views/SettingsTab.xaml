<UserControl x:Class="LoneEftDmaRadar.UI.Radar.Views.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LoneEftDmaRadar.UI.Radar.Views"
             xmlns:vm="clr-namespace:LoneEftDmaRadar.UI.Radar.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Padding="10">

    <d:UserControl.DataContext>
        <d:DesignInstance Type="{x:Type vm:SettingsViewModel}"
                          IsDesignTimeCreatable="False" />
    </d:UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical"
                    Margin="0,0,0,10">
            <!-- Radar Settings -->
            <GroupBox Header="Radar Settings"
                      Margin="0,0,0,12">
                <StackPanel>

                    <!-- help text -->
                    <TextBlock TextWrapping="Wrap"
                               Margin="0,0,0,12"
                               FontStyle="Italic">
Zoom In: F1 / Mouse Wheel Up 
Zoom Out: F2 / Mouse Wheel Down 
Toggle Fullscreen: F11
                    </TextBlock>

                    <!-- buttons -->
                    <WrapPanel Margin="0,0,0,12">
                        <Button Content="Restart Radar"
                                ToolTip="Restarts the Radar for the current raid instance. Can be useful if stuck on 'waiting for raid start' or the raid is not functioning properly."
                                Command="{Binding RestartRadarCommand}"
                                MinWidth="100"
                                Height="35"
                                Margin="0,0,8,8" />
                        <Button Content="Color Picker"
                                ToolTip="Allows customizing entity colors on the Radar UI."
                                IsEnabled="{Binding ColorPickerIsEnabled}"
                                Command="{Binding OpenColorPickerCommand}"
                                MinWidth="100"
                                Height="35"
                                Margin="0,0,8,8" />
                        <Button Content="Open Config"
                                ToolTip="Opens the config folder."
                                Command="{Binding OpenConfigCommand}"
                                MinWidth="100"
                                Height="35"
                                Margin="0,0,8,8" />
                        <Button Content="Backup Config"
                                ToolTip="Backs up your configuration (Recommended)."
                                Command="{Binding BackupConfigCommand}"
                                MinWidth="100"
                                Height="35"
                                Margin="0,0,8,8" />
                    </WrapPanel>

                    <!-- sliders -->
                    <WrapPanel Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="0,0,16,8">
                            <TextBlock Text="UI Scale"
                                       VerticalAlignment="Center"
                                       ToolTip="Sets the scaling factor for the Radar/User Interface. For high‑resolution monitors you may want to increase this." />
                            <Slider x:Name="UIScaleSlider"
                                    Width="150"
                                    Margin="8,0"
                                    Minimum="0.5"
                                    Maximum="2.0"
                                    SmallChange="0.01"
                                    LargeChange="0.1"
                                    Value="{Binding UIScale, Mode=TwoWay}" />
                            <TextBlock Text="{Binding Value, ElementName=UIScaleSlider, StringFormat=F2}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Margin="0,0,16,8">
                            <TextBlock Text="Radar Max FPS (0 = Unlimited)"
                                       VerticalAlignment="Center" />
                            <Slider x:Name="RadarFpsSlider"
                                    Width="180"
                                    Margin="8,0"
                                    Minimum="0"
                                    Maximum="240"
                                    SmallChange="5"
                                    LargeChange="15"
                                    TickFrequency="15"
                                    IsSnapToTickEnabled="True"
                                    Value="{Binding RadarMaxFPS, Mode=TwoWay}" />
                            <TextBlock Text="{Binding Value, ElementName=RadarFpsSlider, StringFormat='{}{0:F0}'}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </StackPanel>
                    </WrapPanel>

                    <!-- toggles -->
                    <WrapPanel>
                        <CheckBox Content="Show Map Setup Helper"
                                  ToolTip="Toggles the 'Map Setup Helper' to assist with getting Map Bounds/Scaling"
                                  IsChecked="{Binding ShowMapSetupHelper, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Aimview Widget"
                                  ToolTip="Toggles the Aimview 'Widget' that gives you a Pseudo ESP in the radar window. Can be moved."
                                  IsChecked="{Binding AimviewWidget, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Player Info Widget"
                                  ToolTip="Toggles the Player Info 'Widget' that gives you information about the players/bosses in your raid. Can be moved."
                                  IsChecked="{Binding PlayerInfoWidget, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Loot Info Widget"
                                  ToolTip="Toggles the Loot Info 'Widget' that gives you information about valuable loot in your raid. Can be moved."
                                  IsChecked="{Binding LootInfoWidget, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Connect Groups"
                                  ToolTip="Connects players that are grouped up via semi‑transparent green lines. Does not apply to your own party."
                                  IsChecked="{Binding ConnectGroups, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Show Hazards"
                                  IsChecked="{Binding ShowHazards, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Teammate Aimlines"
                                  ToolTip="When enabled makes teammate aimlines the same length as the main player."
                                  IsChecked="{Binding TeammateAimlines, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="AI Aimlines"
                                  ToolTip="Enables dynamic aimlines for AI Players. When you are being aimed at the aimlines will extend."
                                  IsChecked="{Binding AIAimlines, Mode=TwoWay}"
                                  Margin="0,0,0,8" />
                    </WrapPanel>

                    <!-- Widget Font Size Slider -->
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="0,8,0,8">
                        <TextBlock Text="Widget Font Size"
                                   VerticalAlignment="Center"
                                   ToolTip="Font size for loot height indicators (▲▼●) and other radar widgets." />
                        <Slider x:Name="WidgetFontSizeSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="6"
                                Maximum="24"
                                SmallChange="1"
                                LargeChange="2"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                Value="{Binding RadarWidgetFontSize, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=WidgetFontSizeSlider, StringFormat='{}{0:F0} px'}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>

                </StackPanel>
            </GroupBox>

            <!-- Loot -->
            <GroupBox Header="Loot"
                      Margin="0,0,0,12">
                <StackPanel>
                    <WrapPanel Margin="0,0,0,8">
                        <CheckBox Content="Show Loot (F3)"
                                  ToolTip="Toggles displaying Loot on the Radar/ESP."
                                  IsChecked="{Binding ShowLoot, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                        <CheckBox Content="Show Static Containers"
                                  ToolTip="Shows static containers on the map/ESP."
                                  IsChecked="{Binding ShowStaticContainers, Mode=TwoWay}"
                                  Margin="0,0,16,8" />
                    </WrapPanel>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="0,0,16,8"
                                IsEnabled="{Binding ShowStaticContainers}">
                        <TextBlock Text="Container Distance"
                                   VerticalAlignment="Center" />
                        <Slider x:Name="ContainerDistSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="0"
                                Maximum="200"
                                SmallChange="1"
                                LargeChange="10"
                                Value="{Binding ContainerDistance, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=ContainerDistSlider}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="0,0,16,8">
                        <TextBlock Text="Pulse Speed"
                                   VerticalAlignment="Center"
                                   ToolTip="Controls how fast the loot pulse animation grows." />
                        <Slider x:Name="PulseSpeedSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="10"
                                Maximum="100"
                                SmallChange="5"
                                LargeChange="10"
                                Value="{Binding PulseSpeed, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=PulseSpeedSlider, StringFormat=F0}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="0,0,16,8">
                        <TextBlock Text="Pulse Duration (s)"
                                   VerticalAlignment="Center"
                                   ToolTip="Controls how long the loot pulse animation lasts." />
                        <Slider x:Name="PulseDurationSlider"
                                Width="150"
                                Margin="8,0"
                                Minimum="1.0"
                                Maximum="10.0"
                                SmallChange="0.5"
                                LargeChange="1.0"
                                Value="{Binding PulseDuration, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Value, ElementName=PulseDurationSlider, StringFormat=F1}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Quest Helper -->
            <GroupBox Header="Quest Helper"
                      Margin="0,0,0,12">
                <DockPanel LastChildFill="True"
                           Margin="0,4,0,0">
                    <CheckBox Content="Enabled"
                              ToolTip="Toggles the Quest Helper feature. This will display Items and Zones that you need to pickup/visit for quests that you currently have active."
                              IsChecked="{Binding QuestHelperEnabled, Mode=TwoWay}"
                              DockPanel.Dock="Left"
                              Margin="0,0,8,0" />
                    <ListBox ItemsSource="{Binding CurrentQuests}"
                             IsEnabled="{Binding QuestHelperEnabled}"
                             Height="400"
                             ToolTip="Active Quest List (populates once you are in raid). Uncheck a quest to untrack it."
                             ScrollViewer.CanContentScroll="True">

                        <!-- 1) Define the panel *with* virtualization baked in -->
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel VirtualizingStackPanel.IsVirtualizing="True"
                                                        VirtualizingStackPanel.VirtualizationMode="Recycling"
                                                        IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <!-- 2) Your existing template -->
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}"
                                          IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
